name: Unzip src

on:
  push:
    paths:
      - 'src.zip'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  unzip:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout with LFS
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Install unzip
        run: sudo apt-get update && sudo apt-get install -y unzip

      - name: Debug - List files before unzip
        run: ls -la

      - name: Validate src.zip
        run: |
          if ! file src.zip | grep -q "Zip archive"; then
            echo "ERROR: src.zip is not a valid ZIP file!"
            file src.zip
            exit 1
          fi
          echo "ZIP file is valid"

      - name: Unzip src.zip into repository root
        run: |
          unzip -o src.zip
          echo "Unzipped $(find . -type f | wc -l) total files after extraction"

      - name: Remove src.zip
        run: rm -f src.zip

      - name: Debug - List files after unzip
        run: ls -la

      - name: Commit & Push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "actions@github.com"

          git add .
          git commit -m "Unpacked src.zip into repository root" || echo "No changes to commit"
          git push origin HEAD:main || echo "Nothing to push"
